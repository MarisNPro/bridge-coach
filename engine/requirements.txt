fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic==2.7.0
pyyaml==6.0.1
# endplay bundles the DDS double-dummy solver (Apache 2.0).
# Not called by the bidding endpoints; needed once /assess (Phase 1 DDS) lands.
endplay==0.5.11
